# TODO (бэклог)

Живой список **что доделать** по продукту и репозиторию. Канон фаз и гейтов — в [`ROADMAP.md`](ROADMAP.md); текущая карта модулей — в [`ARCHITECTURE.md`](ARCHITECTURE.md). Здесь только расхождения «задекларировано ↔ код» и следующие шаги.

---

## V1 (must-have для гейта пилота)

Критерии пилота см. гейт в `ROADMAP.md` (цитаты / явное «не найдено», инвалидация индекса при удалении, on-prem, human eval).

| Что сделать | Зачем | Статус | Ключевые места |
|-------------|-------|--------|----------------|
| Зафиксировать контракт ответа чата: успех ⇒ непустые `citations`, либо явный режим «в корпусе не найдено» без выдуманных фактов; покрыть граничные кейсы тестами. | Гейт V1: доказуемые ответы. | **Частично** (промпт + extractive fallback, см. `CHANGELOG`) | `app/chat_service.py`, `app/rag_runtime.py`, `app/tests/test_chat_service.py` |
| В пилотной/прод конфигурации: при `ALLOW_LLM_EGRESS=true` не допускать работу без политики моделей (например обязательный непустой `POLZA_CHAT_MODEL_ALLOWLIST` или отдельный strict-флаг). | Соответствие задумке allowlist; сейчас пустой allowlist = без ограничения по модели. | **Частично** | `app/config.py`, `app/routers/api.py`, `app/.env.example` |
| Провести human eval на 20–50 вопросах заказчика; зафиксировать выводы (доля ответов с цитатами, отказы, ошибки retrieval). | Прямо в гейте V1; код этот шаг не заменяет. | **Нет** (процесс) | UI/API чата, при желании снимки `tests_favorite`, `/v1/rag-test/*` |
| Убедиться, что пилотный деплой воспроизводим: чеклист env (ключи, `APP_DATA_DIR`, egress, allowlist, backup SQLite+Chroma). | Гейт «on-prem» без сюрпризов. | **Покрыто** инструментами; чеклист в доках можно сжать в один абзац | `README.md`, Docker, `app/.env.example` |

---

## V1 nice-to-have

| Что сделать | Зачем | Статус | Ключевые места |
|-------------|-------|--------|----------------|
| Интеграционный тест: ingest → удаление документа → в Chroma нет чанков по `document_id`. | Страховка под гейт инвалидации индекса. | **Частично** (логика есть; отдельный e2e можно усилить) | `app/routers/api.py`, `app/chroma_store.py`, `app/tests/test_api.py` |
| Короткий ops-абзац «минимум для пилота» (если не хватает в `README`). | Быстрый старт без чтения всего troubleshooting. | **Частично** | `app/docs/README.md`, `troubleshooting.md` |

---

## V2

Кратко — из `ROADMAP.md` (не в ближайшем горизонте V1): brief/report/summary из шаблонов; Marp + цитаты; ACL групп; read-only sharing; **job queue** (парсинг, реиндекс); retention; опционально **BM25+vector**; **eval harness** (golden/regression). Сейчас в коде есть зачаток eval: `/v1/rag-test/*`, избранное, A/B — не полноценный harness V2.

---

## Enterprise

Из `ROADMAP.md`: SSO (SAML/OIDC), группы → ACL, расширенный audit / SIEM, retention по политике, тенанты/метки чувствительности, шифрование/KMS, HA/backup/health, controlled export, snapshots. В текущем `app/` — не целевой скоуп.

---

## Техдолг / тесты / доки

| Что сделать | Зачем | Статус | Ключевые места |
|-------------|-------|--------|----------------|
| Удалить или заменить placeholder-тест осмысленным сценарием. | Явный техдолг в `ROADMAP.md`. | **Нет** (файл-заглушка) | `app/tests/test_placeholder.py` |
| По мере роста API и потоков — не забывать подтягивать `ARCHITECTURE.md`. | Один источник по устройству системы. | **Покрыто** сейчас; держать в актуале | `ARCHITECTURE.md` |
| Расширять регрессию по публичным контрактам (ingest, RBAC, export, audit) по мере изменений. | Правило проекта про тесты на новые пути. | **Частично** (есть `test_api.py`, `test_rag_test_api.py`, `test_ingest.py` и др.) | `app/tests/` |

---

## Следующий шаг (кратко)

1. Формализовать гейт цитат / «не найдено» + тесты.  
2. Ужесточить политику allowlist при включённом egress для пилота.  
3. Запланировать human eval и убрать `test_placeholder.py`.
